import React from "react";
export default function Address({ user }) {
  const handleAddressClick = () => {};
  return (
    <div id="AddressPage">
      <button onClick={() => handleAddressClick()}>Thêm địa chỉ</button>
    </div>
  );
}
